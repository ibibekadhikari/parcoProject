module.exports = {
  theme: {
    extend: {
      colors: {
        customBlue: '#1E40AF',  // Custom color (blue)
        customGreen: '#10B981', // Custom color (green)
        customOrange: '#FB923C', // Custom color (orange)
        // Add any other custom colors you need
      },
    },
  },
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
